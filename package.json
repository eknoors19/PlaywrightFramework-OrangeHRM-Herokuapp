{
  "name": "playwrightassignment",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "allTest": "npx playwright test",
    "allure:generate": "npx allure generate ./allure-results --clean && allure serve",
    "apiTests": "npx playwright test --grep @apitests",
    "uiTests": "npx playwright test --grep @uitests"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": "",
  "devDependencies": {
    "@playwright/test": "^1.45.2",
    "@types/node": "^20.14.11",
    "allure-playwright": "^3.0.0-beta.7"
  }
}
